---
description: "依赖管理规则 — 安装、锁文件、安全审计、升级策略"
globs: ["package.json", "package-lock.json", "pnpm-lock.yaml", ".npmrc"]
---

# 依赖管理规则

## 一、安装原则

- `package-lock.json` 必须提交到 Git
- CI/部署用 `npm ci`（非 `npm install`）
- 不得手动编辑锁文件

## 二、安全审计

```shell
npm audit
```

- critical / high → 24小时内处理
- moderate → 下次发版前处理

## 三、AI 行为约束

- 不得 `npm install` 未经用户确认的包
- 不得删除锁文件后重新生成
- 安装新包后必须一起 commit 锁文件
