---
description: "测试纪律 — 测试金字塔、覆盖率、E2E、回归测试"
globs: ["**/*.test.ts", "**/*.test.tsx", "e2e/**", "vitest.config.*"]
---

# 测试纪律

## 一、测试金字塔

```
       /  E2E  \          ← 少量
      / 集成测试 \         ← 适量
     /  单元测试  \        ← 大量
```

## 二、覆盖率目标

| 模块类型 | 最低覆盖率 |
|---------|-----------|
| 核心业务逻辑（lib/） | 80% |
| API routes | 70% |
| UI 组件 | 60% |
| 工具函数 | 90% |

## 三、回归测试纪律

每个 bug 修复必须附带回归测试：

1. 先写一个失败的测试（复现 bug）
2. 修复 bug
3. 验证测试变绿
4. 一起 commit

## 四、AI 行为约束

- 修 bug 时先写回归测试
- 新功能关键逻辑必须有测试
- 不得为了 CI 通过而削弱测试
