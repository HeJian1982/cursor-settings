---
description: "项目核心规则（最高优先级）— Cursor AI 协作铁律"
alwaysApply: true
---

# 零、项目身份卡片

`yaml
项目名: 何健个人网站
当前版本: v2.21
工作区路径: e:\HJ\Web
框架: Next.js 14 + React 18 + TypeScript 5 + Tailwind 3 + Framer Motion 10.16
运行时: Node >= 20
主分支: main
Git 远端: https://github.com/HeJian1982/hj1982.cn.git
部署环境: 阿里云 ECS + 宝塔 + PM2
站点域名: hj1982.cn
作者: 何健
`

## 一、绝对禁止

1. **不得硬编码密钥** → 一律环境变量
2. **不得擅自修改 .env.local** → 改动前必须与用户确认
3. **不得移动用户个人工作目录**
4. **不得使用阻塞式交互**（CI 友好）
5. **不得杜撰 API/函数/字段** → 必须查证
6. **不得修改与任务无关的代码**
7. **不得删减或弱化已有测试**

## 二、每次必做

1. 修改代码后运行质量检查（typecheck / lint / 相关测试）
2. 生成新文件时保留**版权声明头**
3. 涉及版本/部署的改动，同步更新 CHANGELOG.md
4. 提交前检查根目录是否有需要归档的散落文件
5. **每条用户指令完成后**执行 git add + commit + push

## 三、遇到不确定

- 先用搜索找证据，不凭记忆
- 版本/日期/路径/API 参数不确定时，必须查证
- 用户提供的路径/常量 → 原样使用
