---
description: "文本编码与换行规范 — Windows/PowerShell编码陷阱、mojibake识别与修复"
---

# 文本编码与换行规范

## 一、项目默认（铁律）

1. 普通源代码、配置、文档统一使用 **UTF-8（无 BOM）+ LF**
2. PowerShell 脚本（.ps1）使用 **UTF-8 + BOM**（Windows PowerShell 5.1 兼容性要求）
3. .bat/.cmd 文件保持 **CRLF**
4. `.gitattributes` 锁定换行符行为
5. `.editorconfig` 显式声明换行符行为

### `.editorconfig` 最小模板

```ini
root = true
[*]
charset = utf-8
end_of_line = lf
indent_style = space
indent_size = 2
insert_final_newline = true

[*.ps1]
end_of_line = lf

[*.bat]
end_of_line = crlf
```

## 二、PowerShell 编码陷阱

Windows PowerShell 5.1 默认编码是系统 ANSI（中文 Windows = GBK），写入中文会乱码。

### 写 PowerShell 脚本（必须带 BOM）

```powershell
# ❌ 错误：默认编码写中文会乱码
"中文内容" > file.ps1
"中文内容" | Out-File file.ps1

# ✅ 正确：使用 .NET UTF-8 BOM 编码
$utf8Bom = New-Object System.Text.UTF8Encoding $true
[System.IO.File]::WriteAllText('file.ps1', $content, $utf8Bom)
```

### 写普通文本文件（无 BOM）

```powershell
# ❌ 错误：默认编码写中文会乱码
"中文" > file.txt
"中文" | Out-File file.txt

# ✅ 正确：无 BOM UTF-8
$utf8NoBom = New-Object System.Text.UTF8Encoding $false
[System.IO.File]::WriteAllText('file.txt', '内容', $utf8NoBom)
```

## 三、AI 写脚本时

1. `.ps1` 文件：用 `$utf8Bom` 编码，确保带 UTF-8 BOM
2. `.bat/.cmd`：保持 CRLF，可用 `echo.` 或 Here-String
3. 禁止用 `>` / `Out-File` / `Set-Content` 默认编码写含中文的 .ps1 文件
4. PowerShell 多语句用 `;` 不用 `&&`（PS 5 不支持）
