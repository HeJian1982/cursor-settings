---
name: daily-summary
slug: daily-summary
version: 1.0.0
description: "读取当日 Cursor 对话记录（transcript），整理成每日摘要日志。每条用户指令完成后自动追加到当日日志；每天开始时汇总昨日摘要。"
---

# Cursor AI 每日会话摘要 Skill

本 Skill 负责将每次对话的记录整理归档，形成可追溯的每日日志。

## 核心文件

- **原始记录**：`C:\Users\HJ2\.cursor\projects\e-HJ-cursor\agent-transcripts\<uuid>\<uuid>.jsonl`
- **每日日志**：`e:\HJ\cursor\cursor-transcripts\YYYY-MM-DD.md`

## 流程一：每条对话完成后 — 追加日志（自动执行）

每次用户指令完成（Commit + Push 之后），立即将本轮对话的关键信息追加到当日日志文件。

### 操作步骤

1. 读取当日日志文件（路径 `e:\HJ\cursor\cursor-transcripts\YYYY-MM-DD.md`，不存在则创建）
2. 从 `C:\Users\HJ2\.cursor\projects\e-HJ-cursor\agent-transcripts\` 下找到最新的 transcript 文件
3. 解析 JSONL，提取：
   - 用户请求（user message）
   - AI 执行的关键动作（Write/Delete/StrReplace 等文件操作）
   - 最终结果/结论
4. 按以下格式追加到日志：

```markdown
### HH:MM 左右
**会话 ID**: `<uuid>`

| 项目 | 内容 |
|------|------|
| 主题 | <用户请求的简短描述> |
| 触发条件 | 用户主动发起 / 任务链触发 |
| 动作 | <做了什么，1-3句话> |
| 结果 | <完成状态，1-2句话> |

#### 改动文件
- `<file>` — <改动简述>

#### 验证
- typecheck: ✅ / ❌
- lint: ✅ / ❌
- git commit: `<hash>` / 未提交
```

### 日志追加规则

- **同一次会话**中的多条用户指令，作为同一个 `### HH:MM` 条目下的多个子段落
- **不同会话**（Cursor 重启后），作为新的 `### HH:MM` 条目
- 日志文件末尾的 `---` 分隔线在追加后保持
- 每条追加内容不超过 200 字，聚焦"做了什么 + 结果"，不粘贴完整输出

## 流程二：每天开始 — 生成昨日摘要（按需执行）

当用户说"整理昨天的日志"或"生成昨日摘要"时执行。

### 操作步骤

1. 确定昨日日期：`YYYY-MM-DD`
2. 读取昨日日志：`e:\HJ\cursor\cursor-transcripts\YYYY-MM-DD.md`
3. 读取昨日所有 transcript 文件（按时间戳排序）
4. 提炼出：
   - 昨日共处理了哪些任务
   - 每项任务的结果
   - 未完成/待跟进的事项
   - 从中发现的问题或改进点（可选）

### 输出格式

```markdown
# YYYY-MM-DD 每日摘要

## 任务概览
| # | 主题 | 结果 | 时段 |
|---|------|------|------|
| 1 | <主题> | ✅完成 / ⚠️部分 / 🔄未完成 | HH:MM |

## 详情
### <主题1>
<1-3句话描述>

### <主题2>
<1-3句话描述>

## 待跟进
- [ ] <未完成事项1>
- [ ] <未完成事项2>

## 改进点
- <从昨日工作中提炼的改进建议，可选>
```

## 注意事项

- JSONL transcript 文件可能很大（100k+ tokens），始终用 `tail` 或限制行数读取
- 日志文件不存在时自动创建，带标准头部
- 日志路径硬编码，不得使用变量占位符
- 追加时保留原有内容，只在 `---` 前插入新内容
