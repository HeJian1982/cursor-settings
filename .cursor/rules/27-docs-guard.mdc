# 文档质量门控

AI 文档从"API 通常长什么样"的记忆生成，而非当前代码库的实际。文档是一组关于代码的断言，每个断言都必须可验证。

## 触发时机

- AI 生成或编辑 README、API 文档、docstring、changelog、教程后
- 代码变更影响了已记录的行为
- 发布前

## 核心原则

**每一条文档陈述都是可检查的断言。**

## 自我检查（提交前执行）

1. **每个引用的符号（函数/变量/标志/端点）都在源码中存在** — 不存在则修复或删除该句
2. **代码示例在干净机器上可运行** — 导包路径、API 签名与源码一致
3. **参数名与源码签名匹配** — 读实际代码再写文档，不要从记忆推导
4. **flag/config 值与实际可选项一致** — 不要列举不存在的枚举值
5. **删除无法验证的说法** — "极快"、"世界领先"、"最优" 等无依据的营销语言
6. **changelog 条目对应实际 commit** — 没有 commit 的 changelog 条目是幻觉
7. **版本号与实际发版一致** — 文档说 v2.1 但代码是 v2.0 时更新文档

## 禁止模式

- 引用不存在的 API 或方法
- 复制粘贴 API 文档但不验证
- `@param` / `@returns` 与实际签名不匹配
- 代码示例包含 `// TODO: fill in` 或 `example.com`
- README 最后更新日期早于最后一次代码变更

## 与 clean-code-guard 的配合

- 改了代码行为 → 先跑 `clean-code-guard` 验证代码 → 再跑 `docs-guard` 验证文档
- 文档与代码 drift → 以源码为准更新文档，不以文档为准改代码

## 适用文档类型

README、API 参考、docstring（PHPDoc/JSDoc/tsdoc）、changelog、贡献指南、教程、架构决策记录（ADR）
