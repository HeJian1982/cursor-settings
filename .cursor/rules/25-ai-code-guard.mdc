# AI 代码质量门控

AI 生成代码有可量化的系统性缺陷——在落盘前必须经过质量门控。

## 触发时机

- AI 完成了代码生成、编辑、重构或修复后，提交前
- 用户说 "review this"、"能合并吗"、"重构" 时
- 每次 Verify 阶段（typecheck/lint 通过后）自动执行

## 门控四问（交付前必检）

```
1. 逻辑对吗？  → 函数逻辑、边界条件、null/empty/one/many
2. 风格对吗？  → 匹配文件已有风格（import顺序、错误处理、HTTP客户端）
3. 太复杂吗？  → 圈复杂度 ≤10，嵌套深度 ≤5，参数 ≤4
4. 有幻觉吗？  → import/方法是否真实存在于项目依赖中
```

## 戒律（共 23 条）

### 函数与命名

1. **名字揭示意图** — 禁用 `data`, `data2`, `result`, `item`, `temp`, `value`, `obj`, `info`, `helper`, `manager`, `utils`, `handle_*` / `process_*` / `do_*`（无修饰词）。
2. **函数保持小** — 目标 ≤20 行，一层抽象，一件事。
3. **参数上限 4 个** — 超过立即引入 config/request 对象，禁止 boolean 标志参数。
4. **无输出参数** — 函数要么返回值（查询），要么有副作用（命令），不能两者兼有。命令名用动词，查询名用名词。

### 注释与结构

5. **注释解释为何，不解释何事** — 删除任何重复代码行的注释；删除分步骤注释；删除被注释掉的代码。
6. **匹配文件的现有风格** — 编辑前先读文件本身和至少一个相邻文件；镜像大小写、import 顺序、错误处理方式。

### 设计原则（SOLID）

7. **每个模块只有一个Actor** — 一个类只对一个利益方（会计/认证/报表）负责，两个不相关子系统同时依赖同一个类时拆分。
8. **扩展用新代码，不是改现有代码** — 添加变体需要新的类型分支时，先重构为注册表/策略/多态分发。
9. **子类不拒绝父类契约** — 禁止将 override 用来表示"未实现"或"不支持"；禁止在 override 中加强前置条件或弱化后置条件。
10. **抽象属于调用方，不是实现方** — 引入接口/协议时，放在消费它的包中，不是紧邻具体类。

### DRY / KISS / YAGNI

11. **删除重复的是知识，不是文本** — 两个看起来相似但编码不同规则的不是 DRY 违反；规则在代码+文档+schema 中各表达一次才是。
12. **错误的抽象比重复更糟** — 抽象积累了每个调用者的特例分支时，重新内联回调用方，再删除死分支。
13. **复杂度上限：圈复杂度 ≤10，嵌套深度 ≤5**。
14. **禁止投机** — 禁止无当前调用者的可选参数、config flag、env var、feature toggle、接口、工厂、基类。

### AI 专属防线（最高杠杆）

15. **禁止宽泛的错误吞噬** — 只 catch 可恢复的具体错误类型；不能恢复就让错误上浮；从 catch handler 静默返回 null/none/空成功是禁止的。
16. **禁止对不可能情况的防御性检查** — 不要对声明类型或调用者契约已排除的值加 null 检查或运行时类型检查。
17. **验证每个 import 和外部调用** — 调用库方法前，确认它存在于已安装的版本中；不要基于"应该长这样"生成代码。
18. **禁止硬编码"成功"返回** — 禁止在规范说做实事的函数里返回 `{"status":"ok"}` 或 canned 数据；无法实现就用语言内置的 unimplemented/unsupported 机制显式失败。
19. **重新推导，不从相似代码复制** — 想复制函数并修改时，停下来，从规范重新推导；copy-from-similar 几乎总是引入 off-by-one 和错误 null 语义。
20. **写代码前列出边界情况** — 对任何范围、off-by-one、null/empty/one/many、奇偶、unicode/byte 边界，先写情况列表再实现。
21. **交付前删除死代码** — lint/grep 过一遍未使用的 import、未使用符号、不可达分支、"以防万一" 的 export。
22. **先读再写** — 在不熟悉的仓库写代码前，先读要编辑的文件、一个相邻文件、任何项目规则文件（CLAUDE.md、AGENTS.md、rules/）。

### 重构纪律

23. **重构保留可观测行为** — 用户要求重构时，不要改变契约；相同输入产生相同输出、相同异常、相同副作用、相同排序保证。重构中发现 bug，单独标记并先询问再改。

## 自我检查清单（提交前执行）

- [ ] 遍历戒律 1-23，检查 diff
- [ ] 新函数：行数 ≤20？参数 ≤4？名字揭示意图？
- [ ] 新注释：是否解释为何？如果解释何事则删除
- [ ] 新错误处理：catch 的错误类型具体吗？handler 做了除了静默返回以外的事吗？
- [ ] 新抽象（接口/工厂/基类）：今天有第二个具体使用者吗？没有则内联
- [ ] 读了要编辑的文件和至少一个相邻文件？风格匹配？
- [ ] 有硬编码"ok"返回或 fixture 数据吗？有则替换为真实实现或显式 unsupported 失败
- [ ] 重构：改变了可观测行为吗？改变了则拆分 bug fix 并先询问

## 来源

Clean Code (Robert C. Martin)、Fowler《重构》、Hunt & Thomas《程序员修炼之道》、Sandi Metz、McCabe 复杂度，以及 2024-2026 年 LLM 代码生成的实证研究：
- GitClear 2025：2021-2024 年代码重复增长 8 倍
- USENIX Security '25：16 个模型的包幻觉率平均 19.6%
- arXiv 2411.01414：从相似代码复制引入 off-by-one 错误
