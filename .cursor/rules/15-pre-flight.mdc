---
description: "AI 高风险动作 Pre-Flight Check — Migration/.env/强推/删文件/新依赖"
---

# 高风险动作 Pre-Flight Check

## 一、风险动作总览

| # | 触发动作 | 风险等级 | 必查项 |
|---|---------|---------|--------|
| 1 | 跑 Migration | 🔴 高 | 目标DB、幂等性、影响范围 |
| 2 | 修改 `.env*` | 🔴 高 | 见§二 |
| 3 | `git push --force` / `reset --hard` | 🔴 高 | 见§三 |
| 4 | 删除文件/大块代码 | 🟡 中 | 保护区、范围 |
| 5 | 引入新包依赖 | 🟡 中 | 是否存在、镜像源 |
| 6 | 引入新外部URL/SDK | 🔴 高 | 资源政策 |
| 7 | 启动长进程 | 🟡 中 | 端口冲突 |

## 二、修改 `.env*` 子流程

### 强约束

- ❌ AI 不得自动修改任何 `.env*` 文件
- ❌ 即使用户说"加个环境变量" → 由用户自己改
- ✅ AI 可以创建 `.env.local.template` 的更新版本

## 三、`git push --force` / `reset --hard` 子流程

- ❌ 永远不自动跑
- ❌ 用户历史说过"可以强推" → 当前会话需重新授权
- ✅ 用户当轮明确说"确认强推" → 二次确认后执行

## 四、Pre-Flight 失败 = 硬阻塞

1. 立即停止当前动作
2. 不绕过
3. 告知用户：失败检查项 + 阻塞原因 + 备选方案
4. 等用户决策后才继续
