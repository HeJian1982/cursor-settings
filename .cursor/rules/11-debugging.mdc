---
description: "调试方法论 — 根因分析、最小修复、回归测试、日志策略"
---

# 调试方法论

## 一、调试流程

```
1. 复现 → 2. 定位根因 → 3. 最小修复 → 4. 回归测试 → 5. 验证
```

### 1.1 复现
先确认能稳定复现，记录环境 + 输入 + 预期 + 实际

### 1.2 定位根因
- 先搜索再猜测
- 从错误信息反推
- 不要凭记忆假设原因

### 1.3 最小修复
- 修根因，不修症状
- 能1行改的不改10行

## 二、日志分级

| 级别 | 用途 | 示例 |
|------|------|------|
| error | 影响功能的错误 | DB连接失败、API 500 |
| warn | 可降级但需关注 | 缓存miss |
| info | 关键业务事件 | 用户登录 |
| debug | 调试信息 | 请求参数 |

## 三、常见陷阱速查

| 症状 | 可能根因 | 排查方向 |
|------|----------|----------|
| 本地正常，线上挂 | 环境变量差异 | 对比.env.local |
| 页面白屏 | SSR崩溃/Hydration错误 | 查typeof window守卫 |
| 部署后立即出错 | 依赖版本不一致 | npm ci |

## 四、AI 行为约束

- 不确定根因时不直接改代码
- 修复时说明根因
- commit message 写清"症状+根因"
