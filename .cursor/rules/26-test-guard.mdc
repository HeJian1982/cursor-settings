# 测试代码质量门控

AI 生成测试有系统性失败模式：Mock 滥用、重复测试体、测试验证框架而非逻辑。提交前必须过此门控。

## 触发时机

- AI 刚写完新的测试函数或测试文件后
- 编辑现有测试
- diff 包含测试变更
- 用户说"写测试"、"加测试"、"review 测试"

## 自我检查（提交前执行）

### 通用九条

1. **只在系统边界 Mock** — 不要 Mock 自己项目的状态对象；不要 Mock 内部实现细节。
2. **参数化而非复制粘贴** — 三个测试体只有参数不同 → 合并为一个参数化测试。
3. **删除什么也测不到的测试** — 空 `assert(true)` 或只验证框架自身的测试立即删除。
4. **生产回归测试神圣不可侵犯** — 不得禁用、跳过或削弱已有回归测试。
5. **测试体不验证框架** — 不要 `expect(mockFn).toHaveBeenCalled()` 验证工具函数；测行为，不测实现。
6. **setup/teardown 保持最小** — 超过 20 行的 setup 本身就是一个代码坏味。
7. **集成测试优先** — 能在更高层测就在更高层测；单元测试是补充，不是默认。
8. **错误路径必须有测试** — 正常路径全覆盖了，错误分支呢？
9. **边界情况：null / empty / one / many / 异常值** — 这五类每类至少一个用例。

### AI 专属警示

- 看到 `MagicMock()` 状态、`assert_called_once_with` 在单元测试中大量出现 → 降级为集成测试
- 测试体 90% 相同，只有断言值不同 → 参数化或数据驱动
- 断言日志消息内容而非行为 → 改测行为
- `pytest.raises` 里的异常类型与被测函数真实抛出不匹配 → 修复或删除

## 框架适配（按需读取）

项目使用哪种测试框架，按需参照：

- **Vitest / Jest**: `expect()` 链式断言优先，避免 `toBe` 以外的手动比较
- **Playwright E2E**: 测试独立用户流程，不测 UI 细节；每个测试可单独运行

## 禁止模式

- `test.skip()` 留下未修测试（要么修要么删）
- `describe.skip()` / `it.skip()` 跳过整个套件
- 在测试中 `console.log` 调试后忘记删除
- 测试之间的隐式顺序依赖

## 来源

Spracklen et al., USENIX Security '25；GitClear 2025；Fowler 测试模式研究
