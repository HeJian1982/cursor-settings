# Host: _template

This is the template directory for new machine onboarding.

## What goes here

Place machine-specific Cursor configuration that differs per machine:

- `cursor/settings.json` — Cursor IDE settings (may contain machine-specific paths)
- Any other machine-specific configuration

## Usage

Copy this directory to `hosts/<COMPUTERNAME>/` when onboarding a new machine.
